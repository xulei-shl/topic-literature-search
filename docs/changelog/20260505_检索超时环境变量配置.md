# 开发变更记录
- **日期**: 2026-05-05
- **对应设计文档**: 无

## 1. 变更摘要
为现有检索脚本增加基于 `.env` 的通用超时配置读取能力，统一支持 `PAGE_TIMEOUT`、`NAVIGATION_TIMEOUT`、`CAPTCHA_TIMEOUT`、`ACTION_TIMEOUT`、`PAGE_CHANGE_TIMEOUT`，且不再使用数据库前缀。当前已接入 `cnki-search` 与 `vp-search` 的配置初始化流程。

## 2. 文件清单
- `cnki-search/scripts/config.py`: 修改，增加 `.env` 查找、加载与通用超时解析
- `vp-search/scripts/config.py`: 修改，增加 `.env` 查找、加载与通用超时解析
- `tests/test_search_timeout_config.py`: 新增，覆盖 `.env` 覆盖、显式值优先、非法配置校验
- `cnki-search/README.md`: 修改，补充通用超时环境变量说明

## 3. 测试结果
- [x] 单元测试通过
- [x] 核心路径验证通过
