# 开发变更记录
- **日期**: 2026-05-06
- **对应设计文档**: 无，本次为已确认 Bug 修复

## 1. 变更摘要
修复维普高级检索两处稳定性问题：

- 结果页在整页勾选时新增“已选数量增量校验”，当页级全选异常扩大选中范围时，自动回退到逐条勾选
- 打开高级检索页时避免重复导航，防止点击入口后又立即二次 `goto` 导致导航被中断

## 2. 文件清单
- `vp-search/scripts/interactor.py`: 修改，补充页级全选校验回退逻辑，并优化高级检索页打开流程
- `tests/test_vp_interactor.py`: 修改，新增 2 个回归测试
- `docs/troubleshooting/20260506_维普全选误选与高级检索重复导航.md`: 新增，记录故障复盘

## 3. 测试结果
- [x] 单元测试通过
- [ ] 核心路径验证通过
- 已执行：`python -m pytest tests/test_vp_interactor.py -q`
- 已执行：`python -m pytest tests/test_vp_interactor.py tests/test_vp_export_processor.py tests/test_vp_progress_store.py -q`
- 说明：真实站点命令已复跑，未再出现立即性的重复导航报错，但命令在 120 秒超时窗口内未完成，未做完整线上闭环验证
