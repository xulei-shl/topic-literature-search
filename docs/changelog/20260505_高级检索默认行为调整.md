# 开发变更记录
- **日期**: 2026-05-05
- **对应设计文档**: [docs/需求.md](../需求.md)

## 1. 变更摘要
- 高级检索的 `--deadline-date` 不再默认写死为 `2025-12-31`，仅在显式传参时写入截止日期。
- 补强高级检索页 `中英文扩展` 复选框的取消勾选逻辑，增加事件派发与结果校验。

## 2. 文件清单
- `cnki-search/scripts/cli.py`: 调整 `--deadline-date` 默认值与传参逻辑
- `cnki-search/scripts/interactor.py`: 调整高级检索表单填充与复选框取消勾选逻辑
- `tests/test_cnki_export_processor.py`: 新增 CLI 默认值测试

## 3. 测试结果
- [x] 单元测试通过
- [ ] 核心路径验证通过
