# 开发变更记录
- **日期**: 2026-05-08
- **对应设计文档**: [CNKI 截至年份逐年导出设计文档](/F:/Github/topic-literature-search/docs/design/cnki_date_to_yearly_export_20260508.md)

## 1. 变更摘要
为 `wanfang-search advanced-search` 同步接入“截至年份全量导出逐年编排”能力：

- 当存在 `--date-to` 且没有 `-n/--max-download` 时，切换到逐年模式，不再直接做单次全量分批导出。
- 逐年模式按万方结束年下拉中的真实可选年份生成年度任务。
- 无论是否传入 `--date-from`，每轮都显式把 `date_from/date_to` 写成同一年，按严格单年窗口执行。
- 传入 `--date-from` 时，它只决定从哪一年开始遍历，不再参与累计区间构造。
- 年度子任务继续复用现有批量导出主流程、子进度文件、断点续跑与结果合并能力。
- 对万方“没有命中的记录”页面补充识别，空年份会写 `no-results.txt` 并继续后续年份。

## 2. 文件清单
- `wanfang-search/scripts/wanfang_search_interactor.py`: 修改，接入逐年模式入口分流
- `wanfang-search/scripts/wanfang_yearly_export_ops.py`: 新增，负责逐年编排、总进度与总报告
- `wanfang-search/scripts/wanfang_yearly_progress_store.py`: 新增，负责逐年外层进度文件
- `wanfang-search/scripts/wanfang_form_ops.py`: 修改，复用页面时显式覆盖起始/结束年份默认状态
- `wanfang-search/scripts/wanfang_page_ops.py`: 修改，补充无结果页就绪标记
- `wanfang-search/scripts/result_parser.py`: 修改，支持无结果页按 `total=0` 解析
- `wanfang-search/scripts/utils.py`: 修改，补充逐年模式可读输出
- `tests/test_wanfang_interactor.py`: 修改，新增逐年模式、空年份继续、无结果识别与年份清空测试
- `tests/test_wanfang_progress_store.py`: 修改，新增逐年外层进度文件测试

## 3. 测试结果
- [x] `python -m pytest tests/test_wanfang_interactor.py tests/test_wanfang_progress_store.py -q`
- [x] `python -m pytest tests/test_wanfang_export_processor.py tests/test_wanfang_interactor.py tests/test_wanfang_progress_store.py -q`
- [x] `python -m pytest tests/test_advanced_export_flow.py -q`
- [x] `python -m compileall wanfang-search/scripts src/core`
